# TEMPLATE ONLY (non-paper, non-runnable as-is): run eval_anytime from YAML.
# Copy this file to a local config and fill in real values before use.
# Paper-facing GSM8K router results are reproduced via run_canonical_router_pipeline.sh.
script: scripts/eval_anytime.py
args:
  dataset: gsm8k
  split: test
  max_examples: 200
  budgets: "1,2,3,4"
  # Paper-aligned budget token caps used by bundled GSM8K router artifacts.
  max_new_tokens: "96,160,224,320"
  base_model: YOUR_BASE_MODEL
  adapter_dir: null
  # Template output path (kept separate from canonical paper artifacts).
  save_jsonl: artifacts/template_runs/preds_main_adapter.jsonl
  calibrator: null
