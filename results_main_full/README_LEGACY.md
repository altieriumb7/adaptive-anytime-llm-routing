# Legacy transfer-era placeholders (non-canonical)

This folder is retained for traceability only and is **non-canonical** for manuscript claims.

In this bundled snapshot, key JSONLs in this folder are Git LFS pointers rather than full payloads:
- `preds_main_adapter_full.jsonl`
- `preds_base_full.jsonl`

Reviewer-facing paper artifacts do **not** depend on this folder. Canonical paper-facing transfer table inputs are under `artifacts/router_optionB_boolq/`.
