# BoolQ split payload status (non-canonical placeholders)

This directory name is historical and retained for traceability.

In this bundled snapshot, per-seed BoolQ split files may be Git LFS placeholders. If a file begins with
`version https://git-lfs.github.com/spec/v1`, split-level BoolQ reruns are blocked until LFS payloads are fetched.

The canonical paper-facing BoolQ result is artifact-level from:
- `artifacts/router_optionB_boolq/paper_table_validation_full_per_seed.csv`
